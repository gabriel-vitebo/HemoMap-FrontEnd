import { tv } from "tailwind-variants";

export const schema = tv({
  slots: {
    base: 'bg-sky-900 w-full p-3 flex justify-center items-center gap-4',
    title: 'text-white text-center font-bold text-2xl'
  }
})